# No environment variables are required yet (Phase 1 — static homepage, placeholder data).
# Keys will be added here as each phase introduces them, e.g.:
#   DATABASE_URL=            (Phase 2 — Postgres via Prisma)
#   NEXTAUTH_SECRET=         (Phase 5 — Auth.js)
#   SSLCOMMERZ_STORE_ID=     (Phase 4 — payments)
#   SSLCOMMERZ_STORE_PASSWORD=
#   STEADFAST_API_KEY=       (Phase 6 — shipping)
